# Example .env file for agent_wcag
# Copy to .env and set your real OpenAI key (do NOT commit .env to source control)
OPENAI_API_KEY=
# Optional: default model to use (overridden by UI selector if present)
DEFAULT_MODEL=gpt-3.5-turbo
